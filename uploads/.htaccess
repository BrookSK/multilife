# Permitir acesso ao diretório de uploads
Options -Indexes
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# Permitir acesso a todos os arquivos
<FilesMatch ".*">
    Order Allow,Deny
    Allow from all
    Require all granted
</FilesMatch>
